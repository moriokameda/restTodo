spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3506/todo
    username: root
    password: root
  flyway:
    baseline-version: 0
    enabled: true
    baseline-on-migrate: true

logging:
  level:
    root: info
    web: info
    sql: info